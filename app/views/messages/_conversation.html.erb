<% if conversation.room_members.count > 2 %>
  <%= render partial: 'messages/multi_chat',  locals: { type: type, conversation: conversation, user: user } %>
<% else %>
  <%= render partial: 'messages/single_chat', locals: { type: type, conversation: conversation, user: user } %>
<% end %>
