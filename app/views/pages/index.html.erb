<div class='horizontal content-body'>
  <%= render partial: 'shared/post', collection: @posts %>
  <div>
    <h2>Groups</h2>
    <% @groups.map do |f| %>
      <h4><%= f.name %> - <%= f.user_groups.count %> (thành viên)</h4>
    <% end%>
  </div>
</div>
